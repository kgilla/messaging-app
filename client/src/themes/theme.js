import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  typography: {
    fontFamily: '"Roboto"',
    fontSize: 16,
    h1: {
      fontSize: 40,
    },
  },
  palette: {
    primary: { main: "#3A8DFF" },
  },
});
